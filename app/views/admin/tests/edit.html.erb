<h1>Edit test</h1>
<h1> <%= link_to "#{@test.title} Level #{@test.level}", tests_path %> </h1>
<p>
  <% @test.questions.each do |question| %>
    <p>
      <%= link_to question.body, admin_question_path(question) %>
    </p>
  <% end %>
  <%= render 'form' %>
