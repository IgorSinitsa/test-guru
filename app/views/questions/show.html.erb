<h1><%= link_to @question.body, test_path(@question.test) %></h1>
<div class="links">
  <%= button_to 'Edit', edit_question_path(@question), method: :get %>
  <%= button_to 'Delete', question_path(@question), method: :delete %>
</div>
<% @question.answers.each do |answer| %>
<p>
 <%= answer.body %>
 </p>
<%end%>


